# حالة تنفيذ LEMON

## التشغيل

يعمل المشروع على قالب WebDev كامل المكدس باستخدام React وExpress وtRPC وDrizzle ORM وقاعدة MySQL/TiDB. المصادقة الحالية هي Manus OAuth، وتُنشئ أو تحدّث المستخدم في جدول `users`، بينما يحدد الحقل `role` واجهة CUSTOMER أو DRIVER أو ADMIN. لا توجد حسابات اختبار ثابتة أو أسرار مضمنة في المستودع؛ ينشئ OAuth مستخدماً حقيقياً عند تسجيل الدخول، ويمكن ترقية الحساب الإداري عبر قاعدة البيانات أو واجهة الإدارة.

## المتغيرات

يعتمد المشروع على المتغيرات المضمنة في WebDev: `DATABASE_URL` و`JWT_SECRET` و`VITE_APP_ID` و`OAUTH_SERVER_URL` و`VITE_OAUTH_PORTAL_URL` و`OWNER_OPEN_ID` و`OWNER_NAME` ومتغيرات Forge الخاصة بالخرائط والتخزين والإشعارات. لا يحتاج MVP إلى مفتاح دفع أو متغير دفع خارجي، لأن الطريقة الوحيدة هي الدفع عند الاستلام COD.

## قاعدة البيانات

تم تطبيق migrations الخاصة بجداول المستخدمين وملفات السائقين والمركبات ومناطق الخدمة وقواعد التسعير والطلبات وعروض السائقين وسجل الحالات والتقييمات وسجل التدقيق، مع seed لمنطقة صبيح وقواعد المركبات والأسعار القابلة للتعديل.

## القيود المقصودة

تعمل إشعارات الأطراف عبر abstraction تطويري يسجل الحدث داخل الخادم، بينما يستخدم إشعار المشرف قناة الإشعار المضمنة عند فشل التوزيع. يمكن استبدال provider الداخلي لاحقاً بـ push أو SMS دون تعديل محرك الطلب. الخريطة تفاعلية وتستفيد من proxy المضمن، ولا يتم إنشاء أو عرض GPS حي غير مصدره المستخدم. الدفع الإلكتروني غير متصل في MVP.

## التحقق

نجح `pnpm check`، ونجحت اختبارات logout وآلة حالات الطلب والتسعير وعزل صلاحيات ADMIN، كما نجح `pnpm build`. تم حفظ نسخة مستقرة في checkpoint بعد فحص RTL والتجاوب.
